{% block title %} Crée ton compte {% endblock %}

{% block body %}
    <h1>  form !</h1>
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary">Create!</button>
    {{ form_end(form) }}
{% endblock %}